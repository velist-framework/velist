/**
 * Velist - Type Exports
 * 
 * Import types from here for cleaner imports:
 * ```typescript
 * import type { SharedProps, PageProps, FormErrors } from '$types'
 * ```
 */

export * from './inertia'

// Re-export Elysia types for convenience
export type { ElysiaContext } from './elysia'
